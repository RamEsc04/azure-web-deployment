apiVersion: v1
kind: Service

metadata:
  name: azure-web-app-service
  namespace: web-dev
  labels:
    app: azure-web-app
    environment: dev

spec:
  type: LoadBalancer

  selector:
    app: azure-web-app

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000